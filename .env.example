# LinkToLawyers - Environment Variables for Local Development
# Copy this file to .env and fill in your values

# ===========================================
# DATABASE CONNECTION (Required)
# ===========================================
# Get this from your Replit project's Database settings
DATABASE_URL=postgresql://username:password@host:port/database

# Individual database components (automatically derived from DATABASE_URL on Replit)
PGHOST=your-database-host
PGPORT=5432
PGUSER=your-username
PGPASSWORD=your-password
PGDATABASE=your-database-name

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Port to run the server on (default: 5000)
# On Replit, this must be 5000. For local development, use any port you prefer.
PORT=5001

# ===========================================
# SESSION CONFIGURATION (Required)
# ===========================================
# Secret key for session encryption (generate a random string)
SESSION_SECRET=your-random-session-secret-here

# ===========================================
# LOCAL STORAGE MODE (Optional)
# ===========================================
# Set to 'true' to force local file storage instead of Replit Object Storage
# When running locally (without REPL_ID), this is automatically enabled
USE_LOCAL_STORAGE=true

# ===========================================
# REPLIT OBJECT STORAGE (Only needed on Replit)
# ===========================================
# These are automatically set by Replit when using Object Storage
# PUBLIC_OBJECT_SEARCH_PATHS=/bucket-name/public
# PRIVATE_OBJECT_DIR=/bucket-name/.private
# DEFAULT_OBJECT_STORAGE_BUCKET_ID=your-bucket-id

# ===========================================
# OPENAI API (Required for AI Chat features)
# ===========================================
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# ===========================================
# NOTES FOR LOCAL DEVELOPMENT
# ===========================================
# 1. The app will automatically detect local mode (no REPL_ID)
# 2. Images will be saved to public/uploads/ directory
# 3. Images are served from /images/* endpoint
# 4. Database connection uses the same Replit cloud database
