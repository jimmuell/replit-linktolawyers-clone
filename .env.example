# ===========================================
# LINKTOLAWYERS - Environment Variables
# ===========================================
# This file contains examples of all environment variables needed
# Copy this file to .env and fill in your actual values

# -----------------------------------------
# DATABASE CONFIGURATION (Required)
# -----------------------------------------
# PostgreSQL connection string (from Replit Database)
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://username:password@host:port/database

# Individual database connection parameters (auto-set by Replit)
PGHOST=your-replit-database-host
PGPORT=5432
PGUSER=your-database-username
PGPASSWORD=your-database-password
PGDATABASE=your-database-name

# -----------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------
# Port for the application server
# Default: 5000 (Replit), use 5001 for local development
PORT=5001

# Session secret for encrypting session data
# Generate a random string for production
SESSION_SECRET=your-random-session-secret-here

# -----------------------------------------
# OBJECT STORAGE (Replit only)
# -----------------------------------------
# These are automatically set by Replit Object Storage
# Leave empty for local development (will use local filesystem)
PUBLIC_OBJECT_SEARCH_PATHS=/your-bucket-name/public
PRIVATE_OBJECT_DIR=/your-bucket-name/.private
DEFAULT_OBJECT_STORAGE_BUCKET_ID=your-bucket-id

# -----------------------------------------
# REPLIT ENVIRONMENT (Auto-set on Replit)
# -----------------------------------------
# These are automatically set by Replit, leave empty locally
REPL_ID=
REPLIT_DOMAINS=
REPLIT_DEV_DOMAIN=

# -----------------------------------------
# EXTERNAL SERVICES (Optional)
# -----------------------------------------
# OpenAI API Key for chat and translation features
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key

# SMTP Settings (stored in database, not needed in .env)
# Configure via the admin dashboard

# -----------------------------------------
# NOTES FOR LOCAL DEVELOPMENT
# -----------------------------------------
# 1. Set PORT=5001 to avoid conflicts with Replit's default port
# 2. Leave object storage vars empty - local filesystem will be used
# 3. Images will be saved to ./local-storage/private/uploads/
# 4. Make sure your DATABASE_URL points to the Replit cloud database
# 5. Generate a secure SESSION_SECRET with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
